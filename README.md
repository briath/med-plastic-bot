# 🤖 Med-Plastic Telegram Bot

AI-powered Telegram бот для клиники пластической хирургии "Мед-Пластик" с интеграцией OpenAI GPT-4o-mini и админ-панелью.

## ✨ Возможности

### 🤖 AI-ассистент "Анна"
- **Интеллектуальный диалог**: Бот ведет себя как профессиональный менеджер по продажам
- **OpenAI GPT-4o-mini**: Использует мощную языковую модель для естественного общения
- **Fallback-ответы**: Работает даже без OpenAI через базу знаний
- **Контекстная память**: Помнит историю диалога и учитывает контекст

### 📋 Основные функции
- **Информация об услугах**: Автоматический парсинг сайта med-plastic.ru
- **Запись на консультацию**: Многошаговая форма с валидацией
- **FAQ система**: Ответы на частые вопросы
- **Эскалация**: Перевод на живого менеджера

### 🎛️ Админ-панель
- **Управление заявками**: Изменение статусов, просмотр деталей
- **Статистика**: Графики и аналитика по заявкам
- **Экспорт данных**: Выгрузка в CSV
- **Управление услугами**: Редактирование информации об услугах

## 🏗️ Архитектура

```
med-plastic-bot/
├── handlers/           # Обработчики Telegram бота
├── services/          # Бизнес-логика (LLM, парсер)
├── models/            # SQLAlchemy модели и репозитории
├── states/            # FSM состояния для aiogram
├── keyboards/         # Клавиатуры для бота
├── admin/             # FastAPI админ-панель
├── tests/             # Тесты
├── config/            # Конфигурация
└── utils/             # Утилиты
```

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- Docker & Docker Compose
- Telegram Bot Token
- OpenAI API Key

### 1. Клонирование и настройка

```bash
git clone <repository-url>
cd med-plastic-bot
```

### 2. Конфигурация

Скопируйте файл конфигурации:
```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_telegram_bot_token_here

# ID администратора (для получения уведомлений)
ADMIN_TELEGRAM_ID=your_admin_telegram_id_here

# Настройки OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

# Информация о клинике
CLINIC_NAME=Мед-Пластик
CLINIC_PHONE=+74951234567
CLINIC_EMAIL=info@med-plastic.ru
```

### 3. Запуск с Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f bot
```

### 4. Запуск без Docker

```bash
# Установка зависимостей
pip install -e .

# Запуск бота
python main.py

# Запуск админ-панели (в отдельном терминале)
python admin/main.py
```

## 📖 Использование

### Telegram бот
1. Найдите бота в Telegram по username
2. Нажмите `/start` для начала диалога
3. Используйте кнопки меню для навигации

### Админ-панель
1. Откройте `http://localhost:8000` в браузере
2. Просматривайте заявки и статистику
3. Управляйте услугами и статусами

## 🔧 API

### Эндпоинты админ-панели

- `GET /` - Главная панель со статистикой
- `GET /requests` - Список заявок
- `POST /api/requests/{id}/status` - Обновление статуса заявки
- `GET /api/requests/export` - Экспорт заявок в CSV
- `GET /services` - Управление услугами

### Пример обновления статуса заявки
```bash
curl -X POST http://localhost:8000/api/requests/123/status \
  -H "Content-Type: application/json" \
  -d '{"status": "contacted"}'
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск с покрытием
pytest --cov=.

# Запуск конкретного теста
pytest tests/test_repositories.py -v
```

## 📊 База данных

### Структура таблиц

```sql
-- Пользователи Telegram
users (id, telegram_id, username, first_name, last_name, phone, registered_at)

-- Услуги клиники
services (id, name, description, indications, methods, duration, recovery, price_range)

-- Заявки на консультацию
consultation_requests (id, user_id, service_id, name, phone, preferred_date, comment, status)

-- Логи диалогов
chat_logs (id, user_id, message, response, intent, created_at)

-- База знаний
faq (id, question, answer, keywords, service_id)
```

## 🔐 Безопасность

- **Защита данных**: OpenAI API с безопасной обработкой персональных данных
- **Валидация**: Проверка всех входных данных
- **Аутентификация**: Защита админ-панели (можно добавить)
- **Логирование**: Детальные логи всех действий

## 🚀 Развертывание

### Production с Docker

```bash
# Сборка и запуск
docker-compose -f docker-compose.yml up -d

# Масштабирование
docker-compose up -d --scale bot=2
```

### Переменные окружения для production

```env
LOG_LEVEL=WARNING
DATABASE_URL=sqlite+aiosqlite:///./data/production.db
OPENAI_MODEL=gpt-4o-mini
CLINIC_NAME=Мед-Пластик
```

## 🤝 Вклад в разработку

1. Fork проекта
2. Создайте feature branch
3. Внесите изменения
4. Добавьте тесты
5. Отправьте Pull Request

## 📝 Лицензия

MIT License

## 🆘 Поддержка

- **Telegram**: @admin
- **Email**: support@med-plastic.ru
- **Документация**: [GitHub Wiki](link-to-wiki)

## 🔄 Обновление

```bash
# Обновление кода
git pull origin main

# Пересборка Docker образов
docker-compose build --no-cache

# Перезапуск сервисов
docker-compose up -d
```

## 📈 Мониторинг

### Health checks
```bash
# Проверка бота
curl http://localhost:8000/health

# Проверка OpenAI API
curl -H "Authorization: Bearer $OPENAI_API_KEY" https://api.openai.com/v1/models
```

### Логи
```bash
# Логи бота
docker-compose logs -f bot

# Логи админ-панели
docker-compose logs -f admin
```

## 🎯 Дорожная карта

- [ ] **V2.0**: Мультиязычность (EN, DE)
- [ ] **V2.1**: Интеграция с CRM системами
- [ ] **V2.2**: WhatsApp интеграция
- [ ] **V3.0**: Видеоконсультации через бота
- [ ] **V3.1**: Мобильное приложение для пациентов

---

**Создано с ❤️ для клиники "Мед-Пластик"**
